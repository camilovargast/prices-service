/**
 * Casos de uso: orquestan el dominio y los puertos de salida.
 *
 * <p>Aqui vive la logica de aplicacion (que pasos se dan y en que orden),
 * no la logica de negocio (que significa cada regla), que pertenece al dominio.
 * Puede depender de {@code domain}; nunca de {@code infrastructure}.
 */
package com.bcnc.prices.application;
